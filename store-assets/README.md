# Chrome Web Store listing assets — PromptLens

Everything needed for the Chrome Web Store submission form lives in this folder.

## Build outputs

- `promptlens-chrome-<version>.zip` — generated by `./scripts/pack-chrome.sh`. Upload this zip to the Web Store developer dashboard.

## Required images (Web Store requirements)

| Asset | Size | Status | Notes |
|---|---|---|---|
| Store icon | 128×128 | ✅ Use `apps/chrome/public/icons/icon-128.png` | Ships inside the extension; Web Store auto-reads it |
| Small promo tile | 440×280 | TODO | Required. Brand purple→cyan gradient background + PromptLens mark + tagline |
| Large promo tile | 920×680 | Optional | Higher placement in rotations |
| Marquee tile | 1400×560 | Optional | Requires editor invite; can add later |
| Screenshot 1 — selection overlay | 1280×800 | TODO | Dragging a selection on a Pinterest / Dribbble / Xiaohongshu page |
| Screenshot 2 — Style Prompt result | 1280×800 | TODO | Side panel showing Midjourney / SDXL / Flux / DALL·E tabs |
| Screenshot 3 — Product Visual result | 1280×800 | TODO | Six analysis cards (Color, CMF, Lighting, Composition, Mood, Prompt) |
| Screenshot 4 — Web Design result | 1280×800 | TODO | Tokens overview + Skill.md / Figma export tabs |
| Screenshot 5 — History | 1280×800 | Optional | Local history panel |

> **Screenshot tip**: use a real captured page (1280×800 browser viewport, 100% zoom) with the side panel open. Chrome Web Store expects the PNG to literally show the extension in use — no marketing overlays, no compositing.

## Listing copy

### Name

`PromptLens — Screenshot → AI Prompt, Product Analysis, Design Tokens`

> Max 75 characters. Above is 73.

### Summary / short description (≤ 132 chars)

```
Select any screenshot to get AI prompts, product visual analysis, or webpage design tokens — via your own OpenAI/Anthropic/Gemini key.
```

> 130 chars — under the 132 limit.

### Detailed description

```
PromptLens turns any on-page image — photo, product shot, UI capture — into
structured creative intelligence across three modes:

• Style Prompt — ready-to-paste prompts for Midjourney, Stable Diffusion,
  Flux, and DALL·E / GPT-4o, with --ar, --style, and weighting parameters
  pre-filled.

• Product Visual — six-dimension analysis of any product photo: Color, CMF,
  Lighting + Lens, Composition + Scene, Mood + Props, and a reusable
  generation Prompt.

• Web Design — extract typography, palette, components, interactions, and
  DTCG-compliant design tokens from any page. Export as Skill.md (for AI
  agents) or Tokens Studio JSON (for Figma).

HOW IT WORKS
1. Hit the shortcut (default ⌘⇧Y on Mac, Ctrl+Shift+Y on Windows/Linux).
2. Drag to select any region on the active page.
3. Pick a mode in the side panel, get structured output in seconds.
4. Copy, export to skill.md, or paste straight into your creative tool.

BRING YOUR OWN KEY
PromptLens ships without a server. Plug in OpenAI, Anthropic, Google Gemini,
or any OpenAI-compatible endpoint (Azure, self-hosted, regional clouds). The
request goes from your browser directly to your chosen provider, using your
own API key. Nothing is sent to us.

Today PromptLens is 100% local-first and BYOK. A managed service tier is on
our roadmap — the BYOK option will always remain.

PRIVACY
• No accounts, no telemetry, no backend.
• API key lives in chrome.storage.sync (synced only to your own Google account).
• Images stay in memory; never stored or sent anywhere other than your
  configured endpoint.
• Last 50 generations cached locally in IndexedDB; clear any time.

PERMISSIONS (MINIMUM)
• activeTab — inject the selection overlay on the page you triggered
• storage — remember your API config and local history

Built for designers, brand researchers, product teams, and anyone compounding
a visual vocabulary from scrolling Pinterest, Dribbble, Xiaohongshu, X, or
their own competitor pages.

PromptLens is local-first and open about what it does. Full privacy policy
and source at promptlens.cc.
```

### Category

Primary: `Productivity` · Secondary: `Developer Tools`

### Language

`English` (primary) · also ships `zh-CN`.

### Homepage / Website

`https://promptlens.cc`

### Support URL

`https://promptlens.cc` (or your GitHub repo URL once public)

### Privacy policy URL

Publish `PRIVACY.md` as a standalone web page — recommended at
`https://promptlens.cc/privacy`. The raw markdown file at the repo root is
the source of truth.

### Single-purpose description

```
Convert a user-selected screenshot region into structured AI prompts,
product visual analysis, or design tokens for creative workflows.
```

### Permission justifications

Paste verbatim into the Web Store permission justification fields:

- **activeTab** — "Needed to inject the selection overlay and to call
  `chrome.tabs.captureVisibleTab` on the tab where the user pressed the
  shortcut. Not used to observe browsing or read pages passively."

- **storage** — "Needed to persist the user's provider + API configuration
  (`chrome.storage.sync`) and a local-only prompt/analysis history stored in
  IndexedDB. Never transmitted."

### Data usage disclosure

- **Personally identifiable info:** No
- **Health info:** No
- **Financial / payment info:** No
- **Authentication info:** Yes — the user-supplied API key, stored locally in
  `chrome.storage.sync`, transmitted only to the user-configured endpoint.
- **Personal communications:** No
- **Location:** No
- **Web history:** No
- **User activity:** No
- **Website content:** Yes — the image region the user selects is sent to the
  user's configured API endpoint. Images are not stored on any server run by
  the publisher.

Certifications (check all three):

- [x] I do not sell or transfer user data to third parties, outside of the approved use cases.
- [x] I do not use or transfer user data for purposes unrelated to my item's single purpose.
- [x] I do not use or transfer user data to determine creditworthiness or for lending purposes.

## Submission checklist

Before hitting "Submit" on the Web Store dashboard:

- [ ] `manifest_version: 3` ✅ (already set)
- [ ] Extension version matches `package.json` ✅ (v0.2.0)
- [ ] Icons at 16 / 32 / 48 / 128 all present ✅
- [ ] Short description ≤ 132 chars ✅
- [ ] Screenshots uploaded (at least one, ideally three)
- [ ] Small promo tile (440×280) uploaded
- [ ] Category selected
- [ ] Homepage URL filled in
- [ ] Privacy policy URL filled in (publish `PRIVACY.md` first)
- [ ] Permission justifications pasted
- [ ] Data usage disclosure filled
- [ ] Developer account verified + $5 one-time fee paid
- [ ] Payment profile configured if you want paid features later (optional)
